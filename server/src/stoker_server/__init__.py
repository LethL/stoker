"""Stoker server — FastAPI API, WS-хаб для агентов, state в SQLite."""

__version__ = "0.1.0"


def main() -> None:
    # learn: FastAPI-приложение появится в M1.2 (stoker_server.main:app).
    print(f"stoker-server {__version__}")
